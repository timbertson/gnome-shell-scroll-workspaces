A fork of gfxmonk's gnome-shell-scroll-workspaces, which contains a boolean in extension.js
for refusing to scroll to the last workspace---useful for dynamic workspaces when you don't
want to accidentally scroll to the empty one.
