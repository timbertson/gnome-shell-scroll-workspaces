Change workspaces by scrolling while hovering over the top panel in GNOME shell.

There is one setting, whether or not to ignore the last workspace; this can be useful for those using dynamic workspaces.
